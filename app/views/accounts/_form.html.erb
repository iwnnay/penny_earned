<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, 'Name:' %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :type_of, 'Type:' %>
    <%= f.select :type_of, options_for_select(Account::TYPES) %>
  </div>

  <div class="field">
    <%= f.label :starting_date, 'Starting Date:' %>
    <%= f.text_field :starting_date %>
  </div>

  <div class="field">
    <%= f.label :starting_amount, 'Starting Amount:' %>
    <%= f.text_field :starting_amount %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
